using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Score : MonoBehaviour
{
    public Transform player1; //F. will probably be assigned to score later on.
    //public Transform player2; //W. will probably be assigned to score later on.
    public Text score1Text;
    //public Text score2Text;
    
    
    public Text currentTotalScore; //combined score. hard number for now.

    //// Start is called before the first frame update
    //void Start()
    //{
        
    //}

    // Update is called once per frame
    void Update()
    {
        //Debug.Log(player1.position.z);
        
        //Debug.Log(player2.position.z);
    }
}
